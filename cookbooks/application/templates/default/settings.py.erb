from antler.configs.settings import *

DATABASES = {
    'default': {
        'NAME': '<%= @database['database'] %>',
        'ENGINE': 'django.db.backends.<%= @database['adapter'] %>',
        'USER': '<%= @database['username'] %>',
        'PASSWORD': '<%= @database['password'] %>',
        'HOST': '<%= @db_master %>',
        'PORT': '<%= @database['port']  %>'
    }
}

# TODO: Unhardcode this to being on these same host :/

RESTPOSE_CONFIG = {
    'uri': 'http://127.0.0.1:<%= node['restpose']['port'] %>/',
    'collection': 'antler',
}

CACHES = {
    'default': {
        'BACKEND': 'django.core.cache.backends.memcached.MemcachedCache',
        'LOCATION': '127.0.0.1:<%= node['memcached']['port'] %>',
    }
}